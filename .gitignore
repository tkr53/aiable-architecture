# Go
*.exe
*.test
*.out
go.sum
/capabilities/*/impl/*.so

# Directory where rapid persists failing examples / rapid が失敗例を永続化するディレクトリ
testdata/rapid/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local Claude Code state (not the plugin itself, which lives in .claude-plugin/, skills/, commands/)
.claude/
